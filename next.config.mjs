/** @type {import('next').NextConfig} **/

const nextConfig = {
  images: {
    domains: ["flowbite.s3.amazonaws.com", 'images.unsplash.com', "i.pinimg.com", "cdn.dribbble.com"],
  },
};

export default nextConfig;
